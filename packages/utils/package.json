{
  "name": "@repo/utils",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "echo 'utils lint command placeholder'",
    "test": "echo 'utils test command placeholder'",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  }
}
